# Secrets — never track these
admin.key
qa.key
devconsole.key
devconsole-github.key
get_cf_turn_creds.ps1
turn_creds.env

# QA test case data - lives on the host, not in git (see qa/app.js)
qa-data.json

# Vendored binaries — not our source, just re-download if needed
cloudflared.exe
cloudflared-windows-amd64.exe

# Stray/placeholder files, not meaningful content
curl
public
rename

# Runtime output - not source, regenerated every run
*.log
session-log.jsonl
devconsole-log.jsonl
leads-log.jsonl

# Local dev-server config (machine-specific absolute paths)
.claude/
